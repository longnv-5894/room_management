<div class="page-header">
  <h1 class="header-title"><%= t('operating_expenses.new') %></h1>
  <div class="header-actions">
    <%= link_to t('common.back'), operating_expenses_path, class: "btn btn-outline" %>
  </div>
</div>

<div class="row">
  <div class="col-md-8">
    <div class="card">
      <div class="card-header">
        <h2 class="card-title"><%= t('operating_expenses.expense_details') %></h2>
      </div>
      <div class="card-body">
        <%= render 'form', operating_expense: @operating_expense %>
      </div>
    </div>
  </div>
</div>
